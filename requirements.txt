flask>=3.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
trafilatura>=1.12.0
# lxml >= 5.2 split lxml.html.clean into a separate package, and trafilatura's
# justext dependency still imports it. The [html-clean] extra pulls in the
# split-out module so installs don't fail with "lxml.html.clean is now a
# separate project lxml_html_clean".
lxml[html-clean]>=5.0.0
lxml_html_clean>=0.4.0
